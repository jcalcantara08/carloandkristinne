# dependencies
node_modules/
.pnp
.pnp.js

# next
.next/
out/
build/
next-env.d.ts

# production
dist/

# environment. Never commit any of these.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# credentials. Gitignored, but note these still sync to OneDrive.
ADMIN_CREDENTIALS.md

# The couple's planning workbook and questionnaire. Kept on disk as the
# source of truth for lib/constants.ts, never committed: they contain the
# budget, supplier negotiations and downpayment amounts. This site is for
# guests, and none of that belongs in a repository.
docs/source/

# testing
coverage/
playwright-report/
test-results/

# misc
.DS_Store
Thumbs.db
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vercel
*.tsbuildinfo

# editor
.idea/
.vscode/*
!.vscode/extensions.json

# Claude. The shared config (launch.json, settings.json) IS committed so it
# travels with the repo. Only the machine-local parts are ignored, because
# settings.local.json can capture secrets inside approved-command strings.
.claude/settings.local.json
.claude/worktrees/

# Photo masters (originals, ChatGPT edits, attire guides, invitation suite).
# Kept in the project folder for OneDrive, kept out of git because they are
# large and never served; the web copies live in public/photos.
photos-master/
