# handoff.md

This file has moved. The developer handoff now lives at
[`docs/DEVELOPER-HANDOFF.md`](docs/DEVELOPER-HANDOFF.md), which is the
project's shared memory and the file to read before changing anything.

This pointer stays so that `PROJECT_RULES.md`, `CLAUDE.md`, `README.md` and
the build's own `qa-check` continue to resolve. Do not put content here.
